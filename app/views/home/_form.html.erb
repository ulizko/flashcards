<%= simple_form_for :check, url: check_card_path(@card) do |f| %>
  <%= f.input :check_translate, label: t('.enter_translate'), autocomplete: false %>
  <%= f.hidden_field :time %>
  <%= f.button :submit, t('.check_card')  %>
<% end %>
